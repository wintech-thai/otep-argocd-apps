apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otep-webs-ing
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
    nginx.org/client-max-body-size: 100m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainOtepWeb }}
      secretName: otep-webs-cert
  rules:
    - host: {{ .Values.domainOtepWeb }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: otep-{{ .Values.envGroup }}-otep-webs
                port:
                  number: 80

{{- if eq .Values.envGroup "prod" }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: otep-webs-ing2
  annotations:
    nginx.ingress.kubernetes.io/backend-protocol: HTTP
    nginx.ingress.kubernetes.io/proxy-body-size: 100m
    nginx.org/client-max-body-size: 100m
spec:
  ingressClassName: nginx
  tls:
    - hosts:
      - {{ .Values.domainOtepWeb }}
      secretName: otep-webs-cert2
  rules:
    - host: {{ .Values.domainOtepWeb2 }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: otep-{{ .Values.envGroup }}-otep-webs
                port:
                  number: 80
{{- end }}
