
otep-apis:
  enabled: true
  nameOverride: otep-apis

  ingress:
    enabled: false

  podDisruptionBudget:
    enabled: false

  pods:
    controller: deployment
    #readinessProbe:
    #  httpGet:
    #    path: /health
    #    port: 8080
    #  initialDelaySeconds: 20
    #  periodSeconds: 10
    #  timeoutSeconds: 5
    #  failureThreshold: 2

    #livenessProbe:
    #  httpGet:
    #    path: /health
    #    port: 8080
    #  initialDelaySeconds: 20
    #  periodSeconds: 10

    image:
      name: asia-southeast1-docker.pkg.dev/its-artifact-commons/otep/otep-dashboard-bn

    #volumeMounts:
    #  - name: secrets
    #    mountPath: "/secrets"
    #    readOnly: true

    #volumes:
    #  - name: secrets
    #    secret:
    #      secretName: gcp-sa-secret

  service:
    enabled: true
    ports:
      - port: 80
        targetPort: 3001
        protocol: TCP
        name: http
